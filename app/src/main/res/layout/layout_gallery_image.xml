<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.anthropicandroid.patterngallery.entities.ui.GalleryItemViewModel"/>

        <variable
            name="galleryActionHandlers"
            type="com.anthropicandroid.patterngallery.presenters.GalleryActionHandlers"/>

    </data>

    <FrameLayout
        android:id="@+id/item_image_root"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:elevation="8dp"
        >

        <ImageView
            android:id="@+id/item_image_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onTouch="@{galleryActionHandlers::gridTouched}"
            android:contentDescription="@{viewModel.getDescription()}"
            android:scaleType="centerCrop"
            app:galleryItemViewModel="@{viewModel}"
            />
    </FrameLayout>
</layout>