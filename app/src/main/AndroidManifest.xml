<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.anthropicandroid.patterngallery">

    <application
        android:name="com.anthropicandroid.patterngallery.PatternGalleryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/GalleryTheme">

        <activity
            android:name="com.anthropicandroid.patterngallery.viewmodel.GalleryActivity"
            android:label="@string/gallery_label"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:mimeType="image/svg+xml"/>
            </intent-filter>
        </activity>
    </application>

</manifest>